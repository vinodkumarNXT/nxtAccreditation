import { YearMaskDirective } from './year-mask.directive';

describe('YearMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new YearMaskDirective();
    expect(directive).toBeTruthy();
  });
});
