import { YearMonthDayPipe } from './year-month-day.pipe';

describe('YearMonthDayPipe', () => {
  it('create an instance', () => {
    const pipe = new YearMonthDayPipe();
    expect(pipe).toBeTruthy();
  });
});
